# Base URL of the AlphaMind FastAPI backend.
# Leave unset to run the UI on built-in mock data (no backend required).
NEXT_PUBLIC_API_URL=http://localhost:8000

# Default company shown on first load (ticker OR company name). Not hardcoded.
NEXT_PUBLIC_DEFAULT_TICKER=AAPL
