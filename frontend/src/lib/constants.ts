// Default company shown on first load. Configurable — NOT hardcoded to any one
// company. Override with NEXT_PUBLIC_DEFAULT_TICKER (ticker or company name).
export const DEFAULT_TICKER = process.env.NEXT_PUBLIC_DEFAULT_TICKER ?? "AAPL";
