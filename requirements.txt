# --- Core orchestration ---
langgraph>=0.2.40
langchain-core>=0.3.0
langchain-openai>=0.2.0
openai>=1.40.0

# --- API & UI ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
streamlit>=1.38.0

# --- Data & validation ---
pydantic>=2.7.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.0

# --- Market / news data (graceful fallback if unavailable) ---
yfinance>=0.2.40
httpx>=0.27.0

# --- RAG: SEC filings retrieval ---
langchain-text-splitters>=0.3.0
langchain-qdrant>=0.2.0
qdrant-client>=1.11.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# --- Persistent memory (PostgreSQL + LangGraph memory + vector) ---
sqlalchemy>=2.0
psycopg[binary]>=3.1          # PostgreSQL driver (prod); SQLite used as dev fallback
langgraph-checkpoint-postgres>=2.0   # LangGraph thread/state persistence (optional)

# --- Model Context Protocol (MCP) ---
mcp>=1.2.0                     # MCP Python SDK (clients + our financial server)
langchain-mcp-adapters>=0.1.0  # expose MCP tools as LangChain/LangGraph tools
langgraph>=0.2.40              # create_react_agent for the MCP-powered agent

# --- LLM evaluation framework ---
ragas>=0.2.0                   # faithfulness / retrieval metrics
langsmith>=0.1.100             # tracing, datasets, evaluation runs + feedback

# --- Tooling (optional, dev) ---
pytest>=8.0.0
